# Техническое задание — ToDo

> Источник требований для фронтенда. Макет: Figma (ссылку добавить при необходимости).

---

## Страницы

1. Авторизация  
   Реализовать авторизацию через почту и пароль, а так же на бэкенде реализована jwt авторизация через куки (на фронте реализовать переобновление токенов, выкидывание из сессии если токены просроченные)
2. Домашняя страница
   Блок Header:  
   Отображение ФИ  
   Аватарка (первая буква имени)  
   Уведомления (просто иконка, не кликабельно)

#### Блок Статистика:

Отображение Total (всего задач и выполненные и не выполненные)  
Completed tasks (выполненные задачи)  
Today tasks (сегодняшние задачи)  
Week tasks (недельные задачи)

#### Блок Задачи:

Taday’s tasks (отображение задач на сегодня)

#### Нижняя панель:

Домой (перекидывает на домашнюю страницу) (Активная)  
Календарь (перекидывает на страницу с задачами)  
Список (перекидывает на страницу с time blocking)  
Человечки (открывает страницу с меню)  
Плюсик (на разных страницах имей разных функционал) (перекидывает на страницу с сегодняшними задачами и затем открывает модульное окно для создания задачи на сегодня)

3. Страница с задачами

#### Блок Header:

Кнопка назад (редиректит на прошлую страницу)  
название страницы (зависит от выбранного дня в календаре)  
Уведомления (просто иконка, не кликабельно)

#### Блок Календарь (Возможность выбрать день и открыть расширенный календарь)

#### Блок с приоритетами

Фильтрация по приоритетам: ALL, High, Medium, Low (в зависимости от выбранного приоритета, в блоке задач отображаются отфильтрованные задачи по приоритетам)

#### Блок с задачами (отображаются отфильтрованные по приоритетам)

Задача: Название, время создания, приоритет, input checkbox (можно нажать чтобы завершить задачу)

#### Нижняя панель:

Домой (перекидывает на домашнюю страницу)  
Календарь (перекидывает на страницу с задачами) (Активная)  
Список (перекидывает на страницу с time blocking)  
Человечки (открывает страницу с меню)  
Плюсик (на разных страницах имей разных функционал) (открывает модульное окно для создания задачи на сегодня)

ПРИ НАЖАТИЕ НА +  
Открывается модульное окно для создания новой задачи  
Можно ввести название  
Выбрать приоритет задачи  
Кнопка для подтверждения задачи  
(чтобы выйти из модального окна нужно нажать за пределами окна (на черный фон) или на кнопку крестик (он на месте где был плюс))

4. Pomodoro timer

#### Блок Header:

Кнопка назад (редиректит на прошлую страницу)  
название страницы  
Уведомления (просто иконка, не кликабельно)

#### Блок с таймером:

Таймер (зависит от времени который указан в настройках пользователя (workoutInterval, breakInterval, IntervalCount)  
Время (на бэкенде отдается в секундах и сохраняется в секундах при паузе)

#### Панель с интервалами:

Заполненный квадратик (завершенный интервал)  
Полузакрашенный квадратик (текущий интервал)  
Незакрашенный квадратик (неактивный интервал)

#### Кнопка Reset (сбрасывает текущий круг (обнуляет интервалы))

#### Нижняя панель:

Домой (перекидывает на домашнюю страницу)  
Календарь (перекидывает на страницу с задачами) (Активная)  
Список (перекидывает на страницу с time blocking)  
Человечки (открывает страницу с меню)  
Плей (на разных страницах имей разных функционал) (Запускает и останавливает таймер)

5. time blocking

#### Блок Header:

Кнопка назад (редиректит на прошлую страницу)  
название страницы  
Уведомления (просто иконка, не кликабельно)

Страница пустая

#### Нижняя панель:

Домой (перекидывает на домашнюю страницу)  
Календарь (перекидывает на страницу с задачами)  
Список (перекидывает на страницу с time blocking)  (Активная)  
Человечки (открывает страницу с меню)  
Плюсик (на разных страницах имей разных функционал) (ничего не делает)

6. Меню и настройки

#### Блок Header:

Кнопка назад (редиректит на прошлую страницу)  
название страницы  
Уведомления (просто иконка, не кликабельно)

#### Меню:

Список и страниц

#### Нижняя панель:

Домой (перекидывает на домашнюю страницу)  
Календарь (перекидывает на страницу с задачами)  
Список (перекидывает на страницу с time blocking)  
Человечки (открывает страницу с меню)  
Плюсик (на разных страницах имей разных функционал) (закрывает меню) (Активная)

#### Настройки:

Profile: настройки профиля (email, password, name (имя))  
Pomodoro: work interval (время на рабочий круг), interval Count (кол-во кругов (квадратиков)), break interval (врямя на отдых между рабочими интервалами)

#### Нижняя панель у настроек:

тоже самое что и раньше  
Кнопка принять изменения (на месте где был плюсик)

ОТДЕЛЬНЫЕ МОДУЛИ

#### Кнопка плюсик (много функциональная кнопка, на разных страницах имеет свой функционал)

#### Авторизация:

На бэке есть jwt авторизация, вместе с запросом от бэка автоматом токены встраиваются в cookie браузера  
Управление сессиями через токены ( обновление токенов, проверка токенов на валидность )
